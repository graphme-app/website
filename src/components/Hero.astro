---
// Rotating words inspired by https://github.com/Shroud-email/website/blob/main/src/components/atoms/ScrollingText.vue
---

<section class="bg-white">
    <div class="grid max-w-screen-xl px-4 pb-8 mx-auto lg:gap-8 xl:gap-0 py-8 lg:py-16 lg:grid-cols-12">
        <div class="mr-auto place-self-center lg:col-span-7">
            <h1 class="max-w-2xl text-4xl font-extrabold tracking-tight md:text-5xl">
                <div>Metrics at your fingertips</div>
                <div class="mt-2 text-primary-500">
                    from
                    <div id="words" class="inline pl-2">
                        <span class="absolute">Prometheus</span>
                        <span class="absolute opacity-0 -translate-y-8">Datadog</span>
                    </div>
                </div>
            </h1>
            <p class="max-w-2xl mt-6 font-light text-gray-500 md:text-lg lg:text-xl">
                Visualize time series and collaborate directly from Slack.<br>
                Share insights, reduce troubleshooting time.
            </p>
            <div class="space-y-4 mt-8 sm:flex sm:space-y-0 sm:space-x-4">
                <a href="https://console.graphme.app/signup"
                   class="text-white bg-primary-800 hover:bg-primary-900 focus:ring-4 focus:ring-primary-300 font-medium rounded-lg px-5 py-3 focus:outline-none"
                >
                    Get started now
                </a>
            </div>
        </div>
        <div class="hidden lg:mt-0 lg:col-span-5 lg:flex">
            <div class="bg-aubergine p-2 flex flex-col rounded-lg">
                <img src="/images/slack-logo.png" class="h-8 object-scale-down mb-2" alt="Slack logo"/>
                <img src="/images/screenshot.png" class="object-scale-down rounded" alt="Screenshot from GraphMe"/>
            </div>
        </div>                
    </div>
</section>


<script>
  const container = document.getElementById('words');
  const words = document.querySelectorAll('#words span');
  let currentIndex = 0;
  console.log(container);
  console.log(words);
  console.log(words.length);
  setInterval(() => {
    words[currentIndex].className = "absolute transition-all duration-700 transform opacity-0 -translate-y-8";
    currentIndex = (currentIndex + 1) % words.length;
    words[currentIndex].className = "absolute transition-all duration-700 transform opacity-100 translate-y-0";
  }, 3000)
</script>